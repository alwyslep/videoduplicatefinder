@* 스캔 설정은 워크벤치 좌측 레일(SourcesRail)로 이관됨 → "/" 는 결과로 리다이렉트. *@
@page "/"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/results", replace: true);
}
